// field_call_closure_keeps_state.n - A closure stored in a field keeps captured state across calls

// Expected:
// 1
// 2

fn counter() {
    var n = 0
    fn inc() {
        n = n + 1
        return n
    }
    return inc
}

struct S {
    f
}

val s = S(counter())
print(s.f())
print(s.f())
