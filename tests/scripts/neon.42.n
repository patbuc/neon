// neon.42.n - Realistic scenario: Configuration map

// Expected:
// localhost
// 8080
// true
// 30

// Application configuration as a map
val config = {
    "host": "localhost",
    "port": 8080,
    "debug": true,
    "timeout": 30
}

// Read configuration values
print config["host"]      // localhost
print config["port"]      // 8080
print config["debug"]     // true
print config["timeout"]   // 30
